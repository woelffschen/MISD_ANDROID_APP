package com.misd.cookapp;

/*
import android.support.test.espresso.UiController;
import android.support.test.espresso.ViewAction;
import android.support.test.espresso.intent.Intents;
import android.support.test.espresso.matcher.ViewMatchers;
import android.support.test.rule.ActivityTestRule;
import android.support.test.runner.AndroidJUnit4;
import android.view.View;
import android.widget.DatePicker;

import org.hamcrest.Matcher;
import org.hamcrest.Matchers;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;

import static android.support.test.espresso.Espresso.onView;
import static android.support.test.espresso.action.ViewActions.click;
import static android.support.test.espresso.action.ViewActions.closeSoftKeyboard;
import static android.support.test.espresso.action.ViewActions.typeText;
import static android.support.test.espresso.assertion.ViewAssertions.matches;
import static android.support.test.espresso.matcher.ViewMatchers.withId;
import static android.support.test.espresso.matcher.ViewMatchers.withText;
*/

/**
 * @author Ines Mueller
 */
//@RunWith(AndroidJUnit4.class)
public class RegisterTest {
    /*
    private static String FIRST_NAME;
    private static String LAST_NAME;
    private static String EMAIL;
    private static String STREET;
    private static String POSTALCODE;
    private static String CITY;
    private static String TELEPHONE;

    @Rule
    public ActivityTestRule<RegisterActivity> mActivityRule = new ActivityTestRule<RegisterActivity>(RegisterActivity.class);

    @Before
    public void initValidString() {
        FIRST_NAME = "Max";
        LAST_NAME = "Mustermann";
        EMAIL = "max@test.de";
        STREET = "Musterstrasse 1";
        POSTALCODE = "12345";
        CITY = "Musterstadt";
        TELEPHONE = "0123568964228";
    }

    @Test
    public void testRegisterFirstName() {
        onView(withId(R.id.textRegisterFirstname)).perform(typeText(FIRST_NAME), closeSoftKeyboard());
        onView(withId(R.id.textRegisterFirstname)).check(matches(withText(FIRST_NAME)));
    }

    @Test
    public void testRegisterLastName() {
        onView(withId(R.id.textRegisterLastname)).perform(typeText(LAST_NAME), closeSoftKeyboard());
        onView(withId(R.id.textRegisterLastname)).check(matches(withText(LAST_NAME)));
    }

    @Test
    public void testRegisterEmail() {
        onView(withId(R.id.textRegisterMail)).perform(typeText(EMAIL), closeSoftKeyboard());
        onView(withId(R.id.textRegisterMail)).check(matches(withText(EMAIL)));
    }

    @Test
    public void testRegisterStreet() {
        onView(withId(R.id.textRegisterStreet)).perform(typeText(STREET), closeSoftKeyboard());
        onView(withId(R.id.textRegisterStreet)).check(matches(withText(STREET)));
    }

    @Test
    public void testRegisterPostalCode() {
        onView(withId(R.id.textRegisterPostalCode)).perform(typeText(POSTALCODE), closeSoftKeyboard());
        onView(withId(R.id.textRegisterPostalCode)).check(matches(withText(POSTALCODE)));
    }

    @Test
    public void testRegisterCity() {
        onView(withId(R.id.textRegisterCity)).perform(typeText(CITY), closeSoftKeyboard());
        onView(withId(R.id.textRegisterCity)).check(matches(withText(CITY)));
    }

    @Test
    public void testRegisterTelephone() {
        onView(withId(R.id.textRegisterTelephonenumber)).perform(typeText(TELEPHONE), closeSoftKeyboard());
        onView(withId(R.id.textRegisterTelephonenumber)).check(matches(withText(TELEPHONE)));
    }

    @Test
    public void testRegisterGender() {
        onView(withId(R.id.radiobutton_male)).perform(click());
    }

    @Test
    public void testRegisterBirthday() {
        onView(withId(R.id.birthday_picker)).perform(click());
    }

    @Test
    public void testIfSuccessful() {
        Intents.init();
        onView(ViewMatchers.withId(R.id.user_mail)).check(matches(withText(EMAIL)));
    }*/
}



