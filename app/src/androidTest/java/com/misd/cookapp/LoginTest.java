package com.misd.cookapp;

/*
import android.content.Intent;
import android.support.test.espresso.intent.Intents;
import android.support.test.espresso.matcher.ViewMatchers;
import android.support.test.rule.ActivityTestRule;
import android.support.test.runner.AndroidJUnit4;

import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;

import static android.support.test.espresso.Espresso.onView;
import static android.support.test.espresso.action.ViewActions.click;
import static android.support.test.espresso.action.ViewActions.closeSoftKeyboard;
import static android.support.test.espresso.action.ViewActions.typeText;
import static android.support.test.espresso.assertion.ViewAssertions.matches;
import static android.support.test.espresso.matcher.ViewMatchers.withId;
import static android.support.test.espresso.matcher.ViewMatchers.withText;
*/

/**
 * @author Ines Mueller
 */
//@RunWith(AndroidJUnit4.class)
public class LoginTest {
    /*
    @Rule
    public ActivityTestRule<LoginActivity> mActivityRule = new ActivityTestRule<LoginActivity>(LoginActivity.class);

    @Test
    public void testLogin() {
        onView(withId(R.id.login_mail)).perform(typeText("ines@test.de"), closeSoftKeyboard());
        onView(withId(R.id.textRegisterFirstname)).check(matches(withText("ines@test.de")));
        onView(withId(R.id.login_button)).perform(click());
    }

    @Test
    public void testIfSuccessful() {
        Intents.init();
        onView(ViewMatchers.withId(R.id.user_mail)).check(matches(withText("ines@test.de")));
    }*/

}

